version: "3.6"

services:
  app:
    container_name: karkhana-frontend
    build:
      context: .
      dockerfile: dockerfile
    # volumes:
    #   - /home/bugthedebugger/Documents/Karkhana/Rebranding/Karkhana-Frontend/:/app
    ports:
      - 80:80
    # entrypoint: "npm run start"
